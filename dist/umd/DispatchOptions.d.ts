/// <amd-module name="DispatchOptions" />
/**
 * Options for dispatch
 */
export interface DispatchOptions {
    /**
     * if store the dispatched data for later use
     */
    storeData: boolean;
}
