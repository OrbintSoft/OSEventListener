import UnbindFromEventOptions from './UnbindFromEventOptions';

/**
 * Default options for bind to event.
 */
const DefaultUnbindToEventOptions: UnbindFromEventOptions = {
	/**
	 * As defualt it doesn't throw errors.
	 */
	shouldThrowErrors: false
};

export default DefaultUnbindToEventOptions;